"""Jarvis - a planning/reasoning brain layered over browser-use core tools.

The browser-use library is used purely as a tool layer (CDP + action registry).
No LLM runs inside this package: the MCP host (Claude) is the reasoning engine.
"""

__version__ = '0.1.0'
